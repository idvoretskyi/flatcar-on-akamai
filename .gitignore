# Local environment / secrets
.env
*.tfvars
!terraform.tfvars.example

# Build artifacts
/build/
*.bin.gz
*.bin
ignition.json
*.ign

# OpenTofu / Terraform state and plugins
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
crash.log
tofu.plan
*.tfplan

# Vendored tools
/bin/

# Editor / OS noise
*.swp
.DS_Store

# Live e2e scratch (kubeconfig written by make e2e-k3s / make kubeconfig)
kubeconfig
